/**
 * Punto único de configuración del frontend.
 * Si el sistema se sirve desde la raíz del hosting, dejar tal cual.
 * Si vive en un subdirectorio (ej. /rrhh), ajustar API_BASE_URL.
 */
const API_BASE_URL = '/backend/api';
